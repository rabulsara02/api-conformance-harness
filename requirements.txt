# Direct dependencies only. Transitive deps (pydantic, starlettte, iniconfig, packaging, pluggy,
# Pygments) are resolved automatically by pip and deliberately not pinned here.

# Testing
pytest==9.1.1

# Service under test
fastapi==0.141.1
uvicorn==0.52.1

# HTTP client (used by fastapi.testclient today, by the harness from Day 7)
httpx==0.28.1

# Contract validation + declarative test plans
jsonschema==4.26.0
PyYAML==6.0.3

# Property-based / fuzz testing driven by the contract
schemathesis==4.24.3